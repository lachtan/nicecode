---
paths:
  - "**/*"
description: "Diagnostic rule to verify nicecode rules are loaded"
---

# Nicecode Status Check

When the user says "nicecode status", respond with exactly: `nicecode rules: loaded`
